# Flowra — Local Development Stack
# Run: docker-compose up -d
# API will be at: http://localhost:8301

services:
  # ─── PostgreSQL ──────────────────────────────────────────
  postgres:
    image: pgvector/pgvector:pg16
    container_name: flowra2-db
    environment:
      POSTGRES_USER: flowra
      POSTGRES_PASSWORD: flowra_dev_password
      POSTGRES_DB: flowra
    ports:
      - "5544:5432"
    volumes:
      - flowra2_pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U flowra"]
      interval: 5s
      timeout: 3s
      retries: 5

  # ─── Redis ───────────────────────────────────────────────
  redis:
    image: redis:7-alpine
    container_name: flowra2-redis
    ports:
      - "6381:6379"
    volumes:
      - flowra2_redis:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5

  # ─── MinIO (S3-compatible file storage) ──────────────────
  minio:
    image: minio/minio:latest
    container_name: flowra2-storage
    environment:
      MINIO_ROOT_USER: flowra_access_key
      MINIO_ROOT_PASSWORD: flowra_secret_key
    ports:
      - "9100:9000"    # API
      - "9191:9001"    # Console
    volumes:
      - flowra2_minio:/data
    command: server /data --console-address ":9001"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9000/minio/health/live"]
      interval: 5s
      timeout: 3s
      retries: 5

  # ─── Create default bucket on startup ────────────────────
  minio-init:
    image: minio/mc:latest
    container_name: flowra2-storage-init
    depends_on:
      minio:
        condition: service_healthy
    entrypoint: >
      /bin/sh -c "
        mc alias set flowra http://minio:9000 flowra_access_key flowra_secret_key;
        mc mb flowra/flowra-files --ignore-existing;
        mc anonymous set download flowra/flowra-files;
        echo 'Bucket flowra-files created.';
      "

  # ─── Flowra API ──────────────────────────────────────────
  api:
    build:
      context: .
      target: development
    container_name: flowra2-api
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      minio:
        condition: service_healthy
    environment:
      NODE_ENV: development
      PORT: 8000
      DATABASE_URL: postgresql://flowra:flowra_dev_password@postgres:5432/flowra?schema=public
      JWT_SECRET: dev-jwt-secret-not-for-production
      JWT_REFRESH_SECRET: dev-refresh-secret-not-for-production
      JWT_EXPIRES_IN: 15m
      JWT_REFRESH_EXPIRES_IN: 30d
      OPENAI_API_KEY: ${OPENAI_API_KEY:-sk-your-openai-api-key}
      S3_ENDPOINT: http://minio:9000
      S3_ACCESS_KEY: flowra_access_key
      S3_SECRET_KEY: flowra_secret_key
      S3_BUCKET: flowra-files
      S3_REGION: us-east-1
      LOG_LEVEL: debug
    ports:
      - "8301:8000"
    volumes:
      - ./server/src:/app/server/src       # Hot reload
      - ./shared:/app/shared               # Hot reload
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://localhost:8000/health"]
      interval: 10s
      timeout: 3s
      retries: 5

volumes:
  flowra2_pgdata:
  flowra2_redis:
  flowra2_minio:
