# Docker-compose environment overrides.
# Copy this to .env in the project root before running docker-compose.
# Only the OPENAI_API_KEY needs to be set — everything else has defaults in docker-compose.yml.

OPENAI_API_KEY=sk-your-openai-api-key
